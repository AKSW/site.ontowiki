<?php
// this is the standard template data
$templateData = array(
                'title' => $this->title,
                'resourceUri' => $this->resourceUri,
                'description' => $this->description,
                'descriptionHelper' => $this->descriptionHelper,
                'options'           => $this->options
        );
?>
<div class="box">
    <h2>
        <?php echo $this->title ?>
    </h2>
    <div class="block">
        <div class="article first">
            <?php //TODO: fix this path ?>
            <?php echo $this->partial('generic/content.phtml', $templateData); ?>
            <p class="meta">
                <?php echo $this->partial('generic/lastchange.phtml', $this->lastchange($this->resourceUri)); ?>
            </p>
        </div>
    </div>
</div>
